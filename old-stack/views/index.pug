extends layout

block content
  h1= title
  p This platform holds the collection of open-source resources to learn and grow in different aspects of the technology industry.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    ul
      li #[strong Books:] !{data.book_count}
      li #[strong Authors:] !{data.author_count}
      li #[strong Genres:] !{data.genre_count}